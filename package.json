{
  "name": "stadium-os-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run start",
    "dev": "cd frontend && npm run dev",
    "lint": "cd frontend && npm run lint",
    "test": "cd frontend && npm test"
  },
  "dependencies": {
    "@types/dompurify": "^3.0.5",
    "dompurify": "^3.4.11"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
